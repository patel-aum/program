{"slug":"convert-string-to-pascalcase","title":"Convert string to pascalcase","trackId":4085,"tags":["Java","JavaScript","Kotlin","TypeScript","Python"],"contributors":["rksp25","joao-vitor-souza","Avicenne-b","Dheeraj0014","Arbaz23","VasireddyGanesh","harshraj8843"],"contentHtml":"<h2>Write a program to convert string to pascalcase</h2>\n<p>Pascalcase is a style of writing in which the first letter of each word is capitalized.</p>\n<pre><code>Input  : \"hello world\"\nOutput : \"HelloWorld\"\n</code></pre>\n<hr>\n<pre><code>import java.util.Scanner;\n\npublic class ConvertStringToPascalcase {\n\n    public static void main(String[] args) {\n\n        Scanner sc = new Scanner(System.in);\n        System.out.print(\"saisir le mot ou la phrase : \");\n        String a = sc.nextLine();\n        System.out.println(\"Résultat : \" + toTitleCase(a));\n    }\n\n    public static String toTitleCase(String input) {\n        StringBuilder titleCase = new StringBuilder(input.length());\n        boolean nextTitleCase = true;\n\n        for (char c : input.toCharArray()) {\n            if (Character.isSpaceChar(c)) {\n                nextTitleCase = true;\n            } else if (nextTitleCase) {\n                c = Character.toTitleCase(c);\n                nextTitleCase = false;\n            }\n\n            titleCase.append(c);\n        }\n\n        return titleCase.toString().replaceAll(\"\\\\s\", \"\");\n    }\n\n}\n</code></pre>\n<pre><code>const pascalCase = (string) => {\n  function toPascalCase(string) {\n    return `${string}`\n      .replace(new RegExp(/[-_]+/, \"g\"), \" \")\n      .replace(new RegExp(/[^\\w\\s]/, \"g\"), \"\")\n      .replace(\n        new RegExp(/\\s+(.)(\\w*)/, \"g\"),\n        ($1, $2, $3) => `${$2.toUpperCase() + $3.toLowerCase()}`\n      )\n      .replace(new RegExp(/\\w/), (s) => s.toUpperCase());\n  }\n  const newText = toPascalCase(string);\n  \n  return newText\n};\n</code></pre>\n<pre><code>import java.util.Scanner\n\nfun main() {\n    print(\"Enter String : \")\n    println()\n    var min = readLine()\n\n    println(\"Pasacal Case: \" + ConvertStringToPascalcase(min!!.split(\" \")))\n\n}\n\nprivate fun ConvertStringToPascalcase(args: List&#x3C;String>): String {\n    val buffer = StringBuffer()\n    for((index,day) in args.withIndex()){\n        if(index == 0) {\n            buffer.append(day.get(0).uppercase()).append(day.subSequence(1,day.length))\n        } else {\n            buffer.append(\" \").append(day.get(0).uppercase()).append(day.subSequence(1,day.length))\n        }\n    }\n\n    return buffer.toString()\n}\n</code></pre>\n<pre><code>const toPascalCase = str => (str.match(/[a-zA-Z0-9]+/g) || []).map(w => `${w.charAt(0).toUpperCase()}${w.slice(1)}`).join('');\n\nconsole.log(toPascalCase('hello world'));\n</code></pre>\n<pre><code>input_string = input()\noutput_string = \"\"\nfor word in input_string.split(\" \"):\n    # capitalize() is a buit-in function in python that converts first character in String to uppercase and remaining characters to lowercase\n    output_string += word.capitalize()\nprint(output_string)\n</code></pre>\n","markdown":"\n## Write a program to convert string to pascalcase\n\nPascalcase is a style of writing in which the first letter of each word is capitalized.\n\n```\nInput  : \"hello world\"\nOutput : \"HelloWorld\"\n```\n\n---\n\n<CodeBlock slug=\"convert-string-to-pascalcase\" >\n\n```java\nimport java.util.Scanner;\n\npublic class ConvertStringToPascalcase {\n\n    public static void main(String[] args) {\n\n        Scanner sc = new Scanner(System.in);\n        System.out.print(\"saisir le mot ou la phrase : \");\n        String a = sc.nextLine();\n        System.out.println(\"Résultat : \" + toTitleCase(a));\n    }\n\n    public static String toTitleCase(String input) {\n        StringBuilder titleCase = new StringBuilder(input.length());\n        boolean nextTitleCase = true;\n\n        for (char c : input.toCharArray()) {\n            if (Character.isSpaceChar(c)) {\n                nextTitleCase = true;\n            } else if (nextTitleCase) {\n                c = Character.toTitleCase(c);\n                nextTitleCase = false;\n            }\n\n            titleCase.append(c);\n        }\n\n        return titleCase.toString().replaceAll(\"\\\\s\", \"\");\n    }\n\n}\n```\n\n```js\nconst pascalCase = (string) => {\n  function toPascalCase(string) {\n    return `${string}`\n      .replace(new RegExp(/[-_]+/, \"g\"), \" \")\n      .replace(new RegExp(/[^\\w\\s]/, \"g\"), \"\")\n      .replace(\n        new RegExp(/\\s+(.)(\\w*)/, \"g\"),\n        ($1, $2, $3) => `${$2.toUpperCase() + $3.toLowerCase()}`\n      )\n      .replace(new RegExp(/\\w/), (s) => s.toUpperCase());\n  }\n  const newText = toPascalCase(string);\n  \n  return newText\n};\n```\n\n```kt\nimport java.util.Scanner\n\nfun main() {\n    print(\"Enter String : \")\n    println()\n    var min = readLine()\n\n    println(\"Pasacal Case: \" + ConvertStringToPascalcase(min!!.split(\" \")))\n\n}\n\nprivate fun ConvertStringToPascalcase(args: List<String>): String {\n    val buffer = StringBuffer()\n    for((index,day) in args.withIndex()){\n        if(index == 0) {\n            buffer.append(day.get(0).uppercase()).append(day.subSequence(1,day.length))\n        } else {\n            buffer.append(\" \").append(day.get(0).uppercase()).append(day.subSequence(1,day.length))\n        }\n    }\n\n    return buffer.toString()\n}\n```\n\n```ts\nconst toPascalCase = str => (str.match(/[a-zA-Z0-9]+/g) || []).map(w => `${w.charAt(0).toUpperCase()}${w.slice(1)}`).join('');\n\nconsole.log(toPascalCase('hello world'));\n```\n\n```py\ninput_string = input()\noutput_string = \"\"\nfor word in input_string.split(\" \"):\n    # capitalize() is a buit-in function in python that converts first character in String to uppercase and remaining characters to lowercase\n    output_string += word.capitalize()\nprint(output_string)\n```\n\n</CodeBlock>\n\n","latestUpdateDate":"2023-01-02T18:08:24Z"}